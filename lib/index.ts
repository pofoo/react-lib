import { getClientCoords, getRectSize } from './events';
import { colors, shadow, background } from './constants';
import { mapArrayToObject, changeArrayIndex, getRandomInt } from './data';
import { validateChildren } from './react';

export {
    getClientCoords,
    getRectSize,
    colors,
    shadow,
    background,
    mapArrayToObject,
    validateChildren,
    changeArrayIndex,
    getRandomInt,
}